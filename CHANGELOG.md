# Changelog

## 3.2.0

- Add JSON project specifications through `--spec`, with command-line values taking precedence.
- Add `--dry-run`, `--force`, and `--app-name` for repeatable, safer automation.
- Validate project names and custom paths before generation.
- Restrict the optional native materializer to safe `DIR=` and `FILE=` records.
- Align the standalone `init-app-mcp` catalog with the public CLI contract.

## 3.1.0

- Add AI-native scaffolding and MCP integration support.
